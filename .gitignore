# hf download writes its cache here — never commit it
.cache/
**/.cache/

# OS / editor cruft
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/

# tooling
.playwright-mcp/

# Local-only working docs (launch plans, planning notes) — never published
LAUNCH.md
*.local.md
